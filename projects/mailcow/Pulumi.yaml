name: mailcow
description: Set up a k3s VM running the email services (mailcow) needed for a home lab.
runtime:
  name: nodejs
  options:
    # See https://github.com/TypeStrong/ts-node/issues/1007
    nodeargs: "--loader ts-node/esm --no-warnings"
