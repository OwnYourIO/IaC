name: cloud-proxy
description: Set up a k3s VM running the services needed for a proxy running in the cloud.
runtime:
  name: nodejs
  options:
    # See https://github.com/TypeStrong/ts-node/issues/1007
    nodeargs: "--loader ts-node/esm --no-warnings"
