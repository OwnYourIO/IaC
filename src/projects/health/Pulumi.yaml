name: health-tracking
description: Set up a TechnoCore server running Home Assistant in support of tracking health.
runtime:
  name: nodejs
  options:
    # See https://github.com/TypeStrong/ts-node/issues/1007
    nodeargs: "--loader ts-node/esm --no-warnings"
